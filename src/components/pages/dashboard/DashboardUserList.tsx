import { Box } from '@mui/material';
import React from 'react';

const DashboardUserList = (): JSX.Element => {
    return <Box style={{ maxWidth: '30%', background: 'red', height: '100%' }}>aa</Box>;
};

export default DashboardUserList;
